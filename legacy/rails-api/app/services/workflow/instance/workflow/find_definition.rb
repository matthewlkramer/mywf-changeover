# find my workflow definition (based on localization?)
#   e.g. find the charter workflow if i'm charter?, that puts logic in here.
# base app should determine the right workflow
# once workflow for a worker 
#
# find the instance workflow
#

# across all my workflows, find my startable work, sorted by urgency.
#
#
module Workflow
  class Instance::Workflow
    class FindDefinition
    end
  end
end
