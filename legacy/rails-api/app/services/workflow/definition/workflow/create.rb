module Workflow
  module Definition
    class Workflow
      # create new workflow
      class Create
        # handles inheritance, cloning, versioning.
        # default processes of phase 1, 2, 3, 4, ongoing?
        # that way you can add processes to the side track or within a phase.
      end
    end
  end
end
