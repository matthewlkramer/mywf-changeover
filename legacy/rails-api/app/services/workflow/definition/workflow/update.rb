module Workflow
  module Definition
    class Workflow
      # Update workflow attributes
      class Update
        # handles inheritance, cloning, versioning.
        # update
      # add step
      # reorder step.
      # duplicate (for next version)
      end
    end
  end
end
