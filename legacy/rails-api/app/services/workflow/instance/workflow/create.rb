module Workflow
  class Instance::Workflow
    class Create
      # stamp out the definition as an instance
      # instance is its own graph and state
      # graph can mutate with manual items
    end
  end
end
